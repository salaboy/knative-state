
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: workflows.workflow.knative.dev
spec:
  group: workflow.knative.dev
  names:
    kind: Workflow
    listKind: WorkflowList
    plural: workflows
    singular: workflow
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Workflow is the Schema for the workflows API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkflowSpec defines the desired state of Workflow
            properties:
              workflow:
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  states:
                    additionalProperties:
                      description: State binds a state with an action and a set of
                        events it can handle.
                      properties:
                        events:
                          additionalProperties:
                            description: StateType represents an extensible state
                              type in the state machine.
                            type: string
                          description: Events represents a mapping of events and states.
                          type: object
                      type: object
                    description: States represents a mapping of states and their implementations.
                    type: object
                  version:
                    type: string
                required:
                - states
                type: object
            type: object
          status:
            description: WorkflowStatus defines the observed state of Workflow
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
